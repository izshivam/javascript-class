"use strict"; // treat a JS as newer version

// alert( 3 + 3) // Alert ko use krne ka diff. syntax hai Node ke andar, ye available hai, par esko avi use nhi karenge...... We are using node js, not browser

// max. time when we type codes in React, nodejs , we can avoid semi column(;)

console.log(3 
    +
    3) // code readability should be high

    console.log("Shivam")


let name = "Shivam"
let age = 25
let isLoggedIn = false   
let state;

// number => 2 to power 53
// bigint
// string => ""
// boolean => true/false
// null => standlone value
// undefined =>
// symbol => when is talk about uniqeness


// Object

console.log(typeof "Shivam");
console.log(typeof age);
console.log(typeof isLoggedIn);
console.log(typeof state);
console.log(typeof null);


/** Undefined apne aap me ek type hai, But Null ek Object hai. **/
console.log(typeof undefined); // undefined
console.log(typeof null); // object
